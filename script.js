// Reserved for expand/collapse behaviour on each pillar (LegalTech
// Systems, Learning Repository, Experience) — see the earlier
// prototype for the ArgLex/DPDP expansion pattern to extend here.
document.addEventListener('DOMContentLoaded', () => {
  console.log('Portfolio loaded.');
});
